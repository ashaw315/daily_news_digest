 <div class="container mt-4">
  <h1>Edit Topic</h1>
  
  <%= render 'form', topic: @topic %>
  
  <div class="mt-3">
    <%= link_to 'Show', admin_topic_path(@topic), class: 'btn btn-info' %>
    <%= link_to 'Back', admin_topics_path, class: 'btn btn-secondary' %>
  </div>
</div>